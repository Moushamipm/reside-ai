# Configure pnpm to use a project-local store directory
# This ensures all package dependencies are stored inside the project folder
# rather than in a global user-level cache

store-dir=./.pnpm-store
